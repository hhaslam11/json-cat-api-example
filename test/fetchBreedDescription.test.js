const assert = require('chai').assert;
const fetchBreedDescription = require('../fetchBreedDescription');

describe('#fetchBreedDescription(breed, callback())', () => {
  it('should return correct information when given a valid breed');
});